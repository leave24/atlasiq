"use client";
import {QirEdge,QirNode,displayName} from "../lib/qir";
export function RelationshipsPanel({edges,nodes}:{edges:QirEdge[];nodes:QirNode[]}){const byId=new Map(nodes.map(n=>[n.id,n]));return <section className="panel"><div className="panelHead"><div><span className="eyebrow">QIR EDGES</span><h2>Relationships</h2></div><span className="countBadge">{edges.length}</span></div><div className="relationshipList">{edges.slice(0,80).map((e,i)=><div className="relationshipRow" key={i}><span title={e.from}>{byId.has(e.from)?displayName(byId.get(e.from)!):e.from}</span><code>{e.relationship}</code><span title={e.to}>{byId.has(e.to)?displayName(byId.get(e.to)!):e.to}</span></div>)}</div>{edges.length>80&&<small className="muted">Showing 80 of {edges.length}. Use the graph filters to narrow the architecture.</small>}</section>}
