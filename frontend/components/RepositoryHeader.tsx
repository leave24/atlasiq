"use client";
import {FormEvent} from "react";
export function RepositoryHeader({repository,ref,loading,onRepository,onRef,onAnalyze}:{repository:string;ref:string;loading:boolean;onRepository:(x:string)=>void;onRef:(x:string)=>void;onAnalyze:(e:FormEvent)=>void}){return <><header className="topbar"><div><span className="eyebrow">ARCHITECTURE EXPLORER</span><h1>Architecture Intelligence</h1></div><div className="workspacePill">Default workspace</div></header><form className="scanbar" onSubmit={onAnalyze}><input value={repository} onChange={e=>onRepository(e.target.value)} aria-label="Repository" placeholder="https://github.com/owner/repository" required/><input value={ref} onChange={e=>onRef(e.target.value)} aria-label="Ref" placeholder="Ref / SHA (optional)"/><button className="primary" disabled={loading}>{loading?"Analyzing…":"Analyze repository"}</button></form></>}
